# Cleanup Notes

The repository-ready package was prepared with the following changes:

- Original assignment PDF was excluded.
- Original submission ZIP name containing a student number was not preserved.
- Source files were moved under `assignments/astranova-spaceport-planner/src/`.
- Java build outputs such as `.class` files were removed.
- `.gitignore` files were added for Java build artifacts, IDE files, archives, reports, and generated outputs.
- Example XML and DAT inputs were added for local testing.
- Documentation files were added for GitHub presentation.
- Personal-name and student-number patterns were scanned; none were retained in the final package.
