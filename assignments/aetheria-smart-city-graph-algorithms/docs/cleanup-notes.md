# Cleanup Notes

- Original assignment PDF was excluded from the GitHub package.
- Original submission ZIP name containing a student number was removed.
- No personal name or student number was found in the final source files.
- The project was placed under `assignments/aetheria-smart-city-graph-algorithms/` for the `algorithms` repository.
- Course starter-code dependencies (`Bag.java`, `Edge.java`, `Graph.java`, `Digraph.java`) were not included in the uploaded solution ZIP and are documented as required files.
- Build artifacts and local output files are ignored through `.gitignore`.
