# Cleanup Notes

The GitHub-ready version was prepared with these changes:

- Moved Java source files into `assignments/mediplan-diagnostic-planner/src/`.
- Removed the original submission ZIP name containing a student number.
- Excluded the original assignment PDF from the repository.
- Excluded compiled `.class` files and generated outputs.
- Added a project-level README, Makefile, example XML files, and documentation notes.
- Preserved the original no-package Java source layout so the files can still be compiled directly.

No personal name or student ID is present in the final package.
