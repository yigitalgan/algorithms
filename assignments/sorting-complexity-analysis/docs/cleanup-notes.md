# Cleanup Notes

The original submission archive was reorganized for GitHub.

## Changes Made

- Removed compiled Java bytecode (`Main.class`).
- Moved source code into a standard Maven-style Java source directory.
- Changed the dataset path from a hardcoded root-level CSV to `data/all_stocks_5yr.csv`.
- Added command-line dataset path support.
- Added `pom.xml` for XChart dependency management.
- Added `README.md`, `.gitignore`, dataset instructions, and CV-oriented project notes.
- Excluded the original report PDF because it contained personal/student information.
- Excluded the assignment PDF and dataset.

## Repository Safety

The final package does not include:

- Student number
- Personal name
- Assignment PDF
- Report PDF
- Dataset CSV
- Compiled `.class` files
