# Dataset

The stock-market dataset is not included in this repository.

Place the CSV file here:

```text
data/all_stocks_5yr.csv
```

The program reads the 6th column (`volume`) and benchmarks sorting algorithms on input sizes up to 250,000 rows.
